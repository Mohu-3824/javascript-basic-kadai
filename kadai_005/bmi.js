// 体重（kg）*身長（m）の宣言・値の代入
let weight = 68;
let height = 1.7;

// 身長*身長の値の代入
let height_height = height * height;

// BMIの値の代入
let bmi = weight / height_height;

// コンソールへの出力
console.log(bmi);